#include "RocketTypeBuilder.h"
#include "RocketTypes.h"
#include <string>

using namespace std;

RocketTypes* RocketTypeBuilder::BuildRocketType(string type)
{
    if (type == "Falcon9")
    {
        //create falcon9 if factory is done
    }
    else
    {
        //create falconheavy if factory is done
    }
}